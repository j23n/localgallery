//! Value types and stable identity shared by every LocalGallery frontend.
//!
//! Phase 0 contains only `stable_uuid`; the snapshot/serde types arrive with
//! the phases that need them (see `_plans/00-rust-core-overview.md`).

pub mod stable_uuid;
